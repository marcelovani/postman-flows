{
  "name": "postman-flows",
  "version": "1.0.0",
  "description": "Workaround for running Postman Flows in CI without Enterprise",
  "scripts": {
    "mock": "node mock-server.js",
    "test:mock": "make test-newman-mock",
    "test:newman": "make test-newman",
    "test:newman:flows": "make test-newman-flows"
  },
  "dependencies": {
    "express": "^5.2.1",
    "newman": "^6.2.1",
    "newman-reporter-htmlextra": "^1.23.1"
  }
}
